# المساهمة في مشروع نديم

شكراً لاهتمامك بالمساهمة في مشروع نديم! 🎉

## كيفية المساهمة

### الإبلاغ عن الأخطاء

إذا وجدت خطأً في التطبيق:

1. تحقق من أن الخطأ لم يُبلّغ عنه سابقاً في [Issues](https://github.com/YourUsername/Nadeem/issues)
2. افتح Issue جديد
3. اكتب عنواناً واضحاً ووصفاً مفصلاً
4. أضف خطوات إعادة إنتاج المشكلة
5. أضف لقطات شاشة إن أمكن

### اقتراح ميزات جديدة

لاقتراح ميزة جديدة:

1. افتح Issue جديد
2. ضع علامة "enhancement"
3. اشرح الميزة بالتفصيل
4. اشرح لماذا ستكون مفيدة

### المساهمة بالكود

1. **Fork المشروع**
   ```bash
   git clone https://github.com/YourUsername/Nadeem.git
   ```

2. **أنشئ Branch جديد**
   ```bash
   git checkout -b feature/amazing-feature
   ```

3. **اكتب الكود**
   - اتبع أسلوب Kotlin coding conventions
   - أضف تعليقات واضحة
   - تأكد من أن الكود يعمل بشكل صحيح

4. **Commit التغييرات**
   ```bash
   git commit -m 'Add some amazing feature'
   ```

5. **Push إلى Branch**
   ```bash
   git push origin feature/amazing-feature
   ```

6. **افتح Pull Request**

## معايير الكود

### Kotlin Style Guide

- استخدم Kotlin coding conventions
- استخدم أسماء متغيرات واضحة ومعبرة
- اكتب تعليقات بالعربية أو الإنجليزية

### Compose Best Practices

- استخدم `remember` للحالة المحلية
- استخدم `LaunchedEffect` للعمليات الجانبية
- فصل UI logic عن business logic

### Git Commit Messages

اتبع النمط التالي:

```
نوع: وصف قصير

وصف مفصل (اختياري)

Footer (اختياري)
```

أنواع الـ commits:
- `feat`: ميزة جديدة
- `fix`: إصلاح خطأ
- `docs`: تحديث التوثيق
- `style`: تنسيق الكود
- `refactor`: إعادة هيكلة الكود
- `test`: إضافة اختبارات
- `chore`: مهام صيانة

مثال:
```
feat: إضافة ميزة التذكير اليومي

تم إضافة نظام تذكير يومي للمستخدم لمراجعة حفظه
باستخدام WorkManager و AlarmManager
```

## الاختبار

قبل إرسال Pull Request:

1. تأكد من أن التطبيق يعمل بدون أخطاء
2. اختبر على أجهزة مختلفة إن أمكن
3. تأكد من عدم وجود memory leaks

## الأسئلة؟

إذا كان لديك أي سؤال، يمكنك:
- فتح [Discussion](https://github.com/YourUsername/Nadeem/discussions)
- مراسلتنا على البريد الإلكتروني

شكراً لمساهمتك! 💚
